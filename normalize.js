import { StyleSheet } from 'react-native';

const normalizeStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    // Другие общие стили
  },
  text: {
    fontSize: 16,
    fontWeight: 'bold',
    // Другие общие стили для текста
  },
});

export default normalizeStyles;
